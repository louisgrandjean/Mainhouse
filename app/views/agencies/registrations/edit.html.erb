<body>
  <div class="login-form-bg h-100">
    <div class="container h-100">
      <div class="row justify-content-center h-100">
        <div class="col-xl-6">
          <div class="form-input-content">
            <div class="card login-form mb-0">
              <div class="card-body pt-5">
                <a class="text-center" href="#"> <h2>Mettre votre compte à jour</h2></a>
                  <div class="mt-5 mb-5 login-input">
                  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
                    <%= render "agencies/shared/error_messages", resource: resource %>
                    <div class="field">
                      <%= f.label :email %><br />
                      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: "Email" %>
                    </div>
                    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
                      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
                    <% end %>
                    <br />
                    <div class="field">
                      <%= f.label :nouveau_mot_de_passe %><br />
                      <%= f.password_field :password, autocomplete: "new-password", class: "form-control", placeholder: "Tapez ici votre nouveau mot de passe" %>
                    </div>
                    <br />
                    <div class="field">
                      <%= f.label :confirmation %><br />
                      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control", placeholder: "Confirmez votre nouveau mot de passe" %>
                    </div>
                    <br />
                    <div class="field">
                      <%= f.label :mot_de_passe_actuel %> <br />
                      <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control", placeholder: "Entrez votre ancien mot de passe pour valider"  %>
                    </div>
                    <br />
                    <div class="actions">
                      <%= f.submit "Mettre à jour", class: "btn login-form__btn submit w-100" %>
                    </div>

                  <% end %>
                  
                  <br />
                <h4 class="text-center">Supprimer mon compte</h4>
                <p>En cliquant sur ce lien, vous décidez de supprimer votre compte Mainhouse <%= button_to "Supprimer", registration_path(resource_name), data: { confirm: "Êtes-vous sûr ?" }, method: :delete, class:"btn btn-danger w-100" %></p>
                <%= link_to "Retour", :back %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>